# Copy recipe share link to clipboard

`RecipeFormComponent` contains a Copy To Clipboard button. Add a click handler
to copy the share link to the clipboard.

## Requirements

- Share link is stored inside `shareUrl` signal.
- Add a click handler to copy the share link to the clipboard.
- Use modern JavaScript API - `navigator.clipboard.writeText()`.
- Do NOT use old API.
