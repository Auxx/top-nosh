{
  "$schema": "https://json-schema.org/schema",
  "$id": "Page",
  "title": "Page",
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "description": "Name of a new page",
      "$default": {
        "$source": "argv",
        "index": 0
      },
      "x-prompt": "What name would you like to use for the new page?"
    },
    "project": {
      "type": "string",
      "description": "The project in which to create the page.",
      "x-priority": "important",
      "x-prompt": "Which project the page should be generated in?"
    },
    "feature": {
      "type": "string",
      "description": "The feature in which to create the page.",
      "x-priority": "important",
      "x-prompt": "Which feature the page should be generated in?"
    }
  },
  "required": ["name", "project", "feature"]
}
